<html lang="en">
{{!-- Header --}}
{{>head}}

<body>
  {{!-- Navbar --}}
  {{>navbar}}
  {{!-- Main --}}
  <div class="container">
    {{{ body }}}
  </div>

  {{!-- Footer --}}
  {{>footer}}
</body>

</html>